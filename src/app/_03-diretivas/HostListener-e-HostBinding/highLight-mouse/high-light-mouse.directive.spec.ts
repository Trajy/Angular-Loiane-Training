import { HighLightMouseDirective } from './high-light-mouse.directive';

describe('HighLightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightMouseDirective();
    expect(directive).toBeTruthy();
  });
});
