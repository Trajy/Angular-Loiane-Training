import { PipeImpuroPipe } from './pipe-impuro.pipe';

describe('PipeImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
