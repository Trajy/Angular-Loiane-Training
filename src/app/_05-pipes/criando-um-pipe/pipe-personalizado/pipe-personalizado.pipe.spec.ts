import { PipePersonalizadoPipe } from './pipe-personalizado.pipe';

describe('PipePersonalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePersonalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
