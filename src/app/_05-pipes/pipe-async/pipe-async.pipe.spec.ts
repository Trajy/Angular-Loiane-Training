import { PipeAsyncPipe } from './pipe-async.pipe';

describe('PipeAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
